// the difference between type and T is that we are able to declare type only once per structure
// as well as it remove the need to declare <T> every time we're implementing a trait for structure
/*
impl Iterator for Counter {
    type Item = u32;

    fn next(&mut self) -> Option<Self::Item> {
        // --snip--
 */
